<h2>Hello</h2>

<div class="row">

    <% @products.each do |p| %>
    <div class="col-md-3">
        <div>
            <%= p.title %>
        </div>

        <div>
            <img src="<%= p.path_to_image %>">
        </div>

        <div>
            <%= p.description %>
        </div>

        <div>
            Size: <%= p.size %>
        </div>

        <div>
            Price: <%= p.price %>
        </div>
    </div>
    <% end %>

</div>
<br/>
<button class="btn btn-success" onClick="something()">Click me!</button>